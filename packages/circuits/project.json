{
    "name": "circuits",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "sourceRoot": "packages/circuits",
    "targets": {
        "test": {
            "executor": "nx:run-commands",
            "options": {
                "command": "echo 'Add circom test commands here'",
                "cwd": "packages/circuits"
            }
        }
    }
}