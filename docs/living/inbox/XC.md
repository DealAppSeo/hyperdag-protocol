# XC inbox — overnight refill 2026-09-11 00:56 PDT

/loop 25m. Free only. If this NOW list is empty, copy the next undone line from MESH_GAPS.md or open PRs you own.

## NOW (do in order)
1. ~~trustshell #128 — catch register 429.~~ DONE wakeup 1. Head `22d2de2`. Live `--name pai-night-1` EXIT 0, reused local creds, no stack. `@strix-security` mentioned.
2. init-pai stdout is the demo: Name, Paris PASS, Rome VETO + caught line, RepID, 3 bullets. (429 path printed reuse + Paris PASS + Rome VETO + harness line + RepID 200 + 4 bullets. Polish Name line if still wanted.)
3. Append value-events on register_ok and VETO (reuse scripts/value-events.mjs on #127 if merged, else copy the helper).
4. ~~Keep #126 MERGEABLE.~~ DONE — **MERGED** `1e23a98` (head `7d21ff3`). No rebase. Stranded `baff7a1` recovered as **#130** `87e2794` (12/12 PASS).
5. lastAnchorTx NOT_ANCHORED — add basescan hint for ReputationRegistry 0x8004B663…8713. (NOT_ANCHORED coded on #130; basescan hint still open.)
6. If 1–5 green: smallest engine omit-score for envelope OR leave CLIENT_STRIP test and implement getAllowance reading TrustKeys #7 API if that PR is merged. (CLIENT_STRIP + getAllowance fail-closed are on #130.)

## NOT YOURS
Site page (GA). TrustMarket seed of prod tables (Sean).
