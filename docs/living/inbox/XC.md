# XC inbox — overnight refill 2026-09-11 00:56 PDT

/loop 25m. Free only. If this NOW list is empty, copy the next undone line from MESH_GAPS.md or open PRs you own.

## NOW (do in order)
1. trustshell #128 — catch register 429. Reuse .trustshell/credentials or print name-taken. `--name pai-night-1` second run exit 0.
2. init-pai stdout is the demo: Name, Paris PASS, Rome VETO + caught line, RepID, 3 bullets.
3. Append value-events on register_ok and VETO (reuse scripts/value-events.mjs on #127 if merged, else copy the helper).
4. Keep #126 MERGEABLE (rebase if main moves). cap required in the type.
5. lastAnchorTx NOT_ANCHORED — add basescan hint for ReputationRegistry 0x8004B663…8713.
6. If 1–5 green: smallest engine omit-score for envelope OR leave CLIENT_STRIP test and implement getAllowance reading TrustKeys #7 API if that PR is merged.

## NOT YOURS
Site page (GA). TrustMarket seed of prod tables (Sean).
