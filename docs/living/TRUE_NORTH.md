# TRUE NORTH

Sean-owned. Changes only when direction changes. Contains zero live metrics.
Canon: keep this file identical on `E:\\dev\\living-docs\\TRUE_NORTH.md` and this path. Same hour. trinity-vault is notes, not canon.

## Article 0 — Why

Help people help people. Act justly, love mercy, walk humbly (Micah 6:8).
If survival and truth conflict, choose truth. Fail closed. Never claim live what the running code does not do.
Public README stays secular-honest. This covenant stays in True North.

## Article 1 — The product

The product strangers download is a **thin portable trust harness**: `@hyperdag/trustshell` (+ MCP).
They install it on a new agent or wrap an existing one. It makes that agent private, portable, efficient, and safe enough to build solutions other people and other agents can use.

The spine that must ship together: **zkRepID · HAL · x402 · ERC-8004**.
Those four are the A2A trust ecosystem. TrustMarket, HuggingFace, GCP, AWS, A2A Colony, LobeHub are **downstream distribution**, not the product and not this quarter's build.

Trinity Symphony / T12 is the dogfood factory. It exists to keep the harness honest. Nobody else has to run our twelve agents to use the harness.

v1 is a thin hosted client. The engine sees requests. On-device mesh and "data stays yours" are v2. Use shipped names only: `verifyOutput`, `getRepID`, `presentProof`. `evaluate()` is not on npm 1.3.0. `register()` is not an ERC-8004 mint.

## Article 2 — 90-day gate

First external builder (not Sean, not T12) installs the **published** harness and completes one verified loop:
keyless `verifyOutput()` + keyless `getRepID()` / `presentProof()`, Sean not in the loop.
Evidence: a receipt we can verify + an install that is not our account.

x402 settlement is the next mile (API key + testnet wallet). Not the gate: "MVP launched," 16 boxes, a marketplace listing, KYA, mobile PWA, on-device v2 mesh.
Until that gate, no public launch language.

## Article 3 — Claim = code

Change code to match claims. Do not water down a true claim. Do not ship a claim as live until npm/MCP serves it.
Internal stamps: CLAIMED | BUILT | VERIFIED | BLOCKED.
Landing pages and README may only print VERIFIED.

HAL 6/6, `evaluate()`, on-device mesh, behavioral-integrity enforcement — not live until the published package does them.

## Freeze (until the 90-day gate)

Do not open: TrustMedical / HealthID / PHI, TrustEscrow, TrustSquad, PurposeHub new work, Harmonia, TrustAgent, new landings, TrustKeys-as-product, Halo2, SuperMCP v2, mesh, TrustCRE, AITC revival, AISocialMirror features, TrustMarket UI beyond stub, AWS/GCP/HF marketplace listings, new product folders.
No new surface without a Linear ticket.

## Decision rights

Sean-only: Railway, npm publish, HOLD merges, money/schema/publish, mainnet, credits, outreach, patents, identity SQL, column rename, destructive DDL, SECURITY DEFINER revoke, new domains, public "MVP launched."

Grok + Claude may merge when: unheld + CI green + not money/schema/publish + both agree.
Overnight cast: CC + T12 only.
Phone bus (do not replace): Linear HYP-6 verbs — `yes` / `merge` / `publish` / `wire it` / `PULL-NEXT <id>`.

## Habit contract — do not stop short

The recurring failure is: we almost land it, the session ends, Sean forgets, the next agent starts a new sweep.

Rules:
1. AM inherits yesterday's OPEN loops. No new sweep until OPEN = 0 or every remainder is Sean-only blocked.
2. Vital-never-miss (must appear on every TODAY.md): money-path settlement beat, honesty claim=code, live instrument health, lockstep of these files.
3. A loop is not done until a live instrument that can produce presence says so.
4. Every session ends by writing NEXT on TODAY.md. If tokens remain, execute NEXT. Do not wait for a new prompt.
5. HOLD ≠ idle. HOLD is that branch only.
6. STATE.md is generated. If LAST_VERIFIED > 24h, refuse numbers.
7. Lockstep: edit living-docs and GitHub together. A file that exists on only one side is not canon.
8. Keep looping while free tokens remain, inside the night-forbidden list.

Night forbidden: npm publish, Railway restart, MODE=full, column rename, new product folder, PHI, SECURITY DEFINER revoke, HOLD merge, public launch language, spend, Gemini/ChatGPT inventories.
